<div class="wrapper">
  <% if TeSS::Config.solr_enabled %>
      <div id="sidebar" class="popout-sidebar">
        <%= render partial: "search/common/facet_sidebar", locals:  { resources: @eventunscrapeds_results, resource_type: Eventunscraped } %>
      </div>
  <% end %>

  <div id="content">

    <% if TeSS::Config.solr_enabled %>
        <div class="row">
          <%= render partial: "search/common/search_box", locals: { resource_path: eventunscrapeds_path } %>
          <button class="btn btn-default" id="sidebar-toggle" type="button" autocomplete="off">
            <i class="fa fa-filter"></i> Filters
          </button>
        </div>
        <div class="row">
          <%= render partial: "search/common/sort_by", locals: { resources: @eventunscrapeds, extras: [['Most recent', 'new']] } %>
          <%= render partial: "search/common/search_info", locals: { resources: @eventunscrapeds_results, resource_type: 'eventunscraped' } %>
          <%= render partial: "search/common/search_filters", locals: { resources: @eventunscrapeds_results } %>
        </div>
    <% end %>

    <%= render partial: "search/common/pagination_bar", locals: { resources: @eventunscrapeds } %>

    <hr>

    <%= render @eventunscrapeds %>

    <%= render partial: "search/common/pagination_bar", locals: { resources: @eventunscrapeds } %>

  </div>
</div>
